let a = prompt("enter the value:");
let b = prompt("enter the value:");

if(a>0)
{
    if(b>0)
    {
        document.write("A and B both are positive.")
    }
    else
    {
        document.write("A is Positive but B is not Positive.")
    }
}
else
{
    document.write("A is not positive");
}